const { test, expect } = require('@jest/globals');
//const file = require("../src/scripts/addRecipe.js");
test('extraction test1', async () => {   
    //const extract = await file.checkDup('https://foodista.com/recipe/ZHK4KPB6/chocolate-crinkle-cookies');
    //expect(extract).toBe(false);
});
test('extraction test2', async () => {
    //const extract = await file.checkDup('https://foodista.com/recipe/ZHK4KPB6/chocolate-crinkle-cookies');
    //expect(extract).toBe(false);
});
